# Synthetic Workflows

Controlled LangGraph workflow variants for cache behavior experiments.

Use this directory for small deterministic topologies that isolate prefix reuse,
agent-transition effects, prompt-layout sensitivity, and cache-pressure behavior.
